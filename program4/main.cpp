#include <iostream>
#include <fstream>

using namespace std;

int main() {

    // get input from file

    // as we are getting input, add each word into hash table checking if word already exists in table
    // if it exists, increment the frequency

    // after the whole file has been read, sort the frequencies of each word in descending order
    // assign codes (1...) starting from the highest frequency

    return 0;
}