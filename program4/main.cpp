#include <iostream>
#include <fstream>

using namespace std;

int main() {

    //what does the decoder mapping file contains?
    
    // get input from file

    //store words in vector as WordEntry objects

    //WordEntry class
    //string word
    //int frequency

    //while adding - need logic for checking if word already exists and incrementing frequency if it does

    //sort vector by frequency (descending order)


    return 0;
}